# Aerospace Engineering Portfolio

A static Astro portfolio with an industrial, engineering-documentation visual system.

## Commands

- `npm run dev` — start the local site
- `npm run build` — create the production build
- `npm run preview` — preview the production build

Project content lives in `src/content/projects`. Replace the clearly marked draft
content, profile links, résumé, and media before making the site public.
